# Domain terms that read as prose, not code, in doc comments. ".." keeps
# clippy's built-in allowlist and appends ours.
doc-valid-idents = ["PostgreSQL", "SQLite", "SplitMix64", "BackendKeyData", "RowDescription", "DataRow", "CommandComplete", "ReadyForQuery", "ErrorResponse", "SQLState", ".."]
